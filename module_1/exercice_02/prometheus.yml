global:
  scrape_interval: 10s
  external_labels:
    environment: lab

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

# Ajouté pour l'exercice 3
  - job_name: "node" 
    static_configs:
      - targets: ["172.17.0.3:9100"]
